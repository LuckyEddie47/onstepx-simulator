// minimal.h — No mount, no features, no weather.
// Used to verify simulator starts and firmware probe passes with zero capabilities.
#define HOST_NAME "OnStepMinimal"
// All AXIS models default to OFF (absent = OFF)
